import Header from "./components/Header";

function App() {
  return (
    // JSX expressions must have a single element (div in this case) 
    <div className="container">
      <Header />
    </div>
  );
}

export default App;
